---
description: A startup's numbers — pricing, unit economics (CAC, LTV, payback), a simple model, burn/runway, and the path to default-alive
argument-hint: "<path to project doc, or describe the business>"
allowed-tools: Task, Read, Glob, Grep, WebSearch, WebFetch
---
Build the financial picture for: $ARGUMENTS

If that's a path, read it. Dispatch the `finance-lead` subagent on the business — pass it the full doc or
description — and return its model: pricing, unit economics, a simple conservative/optimistic model,
burn/runway, and the path to default-alive.
