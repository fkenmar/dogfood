---
description: A growth & distribution plan for a startup — first-100-users playbook, the channel to win first, launch, and the growth loop
argument-hint: "<path to project doc, or describe the product & user>"
allowed-tools: Task, Read, Glob, Grep, WebSearch, WebFetch
---
Build a growth & go-to-market plan for: $ARGUMENTS

If that's a path, read it. Dispatch the `growth-marketer` subagent on the product — pass it the full doc
or description — and return its plan: the first 10/100 users, the one channel to win first, the launch,
and the growth loop.
