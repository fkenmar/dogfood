---
description: A pragmatic technical co-founder's build plan — feasibility, stack, architecture, build-vs-buy, technical MVP, and risks
argument-hint: "<path to project doc, or describe what you want to build>"
allowed-tools: Task, Read, Glob, Grep, WebSearch, WebFetch
---
Build a technical plan for: $ARGUMENTS

If that's a path, read it. Dispatch the `technical-founder` subagent on the project — pass it the full
doc or description — and return its build plan.

If the idea is too vague to assess (no clear product or user), point me at `/founder` first to nail down
what's being built, then come back here.
